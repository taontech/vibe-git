# GMC

GMC is a local Git workbench for AI-assisted development.

Start with:

```sh
gmc web
```

The Web dashboard provides:

- Git status, branch state, and commit graph
- Selectable working tree commits
- Repository tasks stored in `.gmc/tasks`
- AI-assisted commit message generation

For full documentation, see `README.md` in the package root.
